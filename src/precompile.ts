// precompile.ts — compatibility shim for older imports

export {
    ParseDriver,
    PrecompileDriver,
    isParseWorker,
    isCompilerWorker,
    runParseWorker,
    runCompilerWorker,
} from './parse';
